# Railway Environment Variables Template
# Copy and paste these into Railway Dashboard → Variables

PORT=3001
NODE_ENV=production

# AWS S3 Configuration (REQUIRED)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1
AWS_S3_BUCKET=
S3_BUCKET_NAME=

# Google Vertex AI Configuration (REQUIRED)
GOOGLE_CLOUD_PROJECT_ID=
VERTEX_AI_LOCATION=us-central1

# Chitu 3D Printer API Configuration (REQUIRED)
CHITU_APP_ID=
CHITU_APP_SECRET=
CHITU_API_URL=https://www.gzchitu.cn
CHITU_DEFAULT_PRODUCT_ID=dZesWMYqBIuCwV1qr6Ugxw==
CHITU_DEFAULT_PAY_TYPE=nayax

# MQTT Configuration (REQUIRED)
CHITU_MQTT_BROKER=open-mqtt.gzchitu.cn
CHITU_MQTT_PORT=1883
CHITU_MQTT_USERNAME=
CHITU_MQTT_PASSWORD=

# Available Machines (REQUIRED)
AVAILABLE_MACHINES=CT0700026

# Security Tokens (REQUIRED - Generate with: openssl rand -hex 32)
ADMIN_AUTH_TOKEN=
CLEANUP_AUTH_TOKEN=
PRINTER_AUTH_TOKENS=

# Frontend URL (Update after Vercel deployment)
FRONTEND_URL=https://your-app.vercel.app

# NOTE: Railway auto-provides these from database addons:
# - DATABASE_URL (from PostgreSQL)
# - REDIS_URL (from Redis)
# You don't need to add them manually!
